const supertest = require('supertest');
const { getUser } = require('../src/controllers/dbController');

describe('User added', () => {
  describe('Valid user added', () => {
    test('should respond with a 200 status code'), () => {};
  });
});
